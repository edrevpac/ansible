---

- hosts: all

  become: true
  
  tasks:

  - name: install nginx
    apt:
      update_cache: yes
      name: nginx
      state: present

  - name: enable nginx service to run on boot
    service:
      name: nginx
      enabled: yes
      state: started